# wakfu_translator
wakfu translator with UI.

1: download python in your machine!
2: run the following command to run application:
  python setup.py    // first time
  python main.py     // to run main application without doing setup
